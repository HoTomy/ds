CREATE TABLE public.comment (
	id serial4 NOT NULL,
	catid varchar(65535) NOT NULL,
	userid varchar(65535) NOT NULL,
	comment varchar(65535) NOT NULL,
	dateregistered timestamp NOT NULL DEFAULT now(),
	CONSTRAINT comment_pkey PRIMARY KEY (id)
);